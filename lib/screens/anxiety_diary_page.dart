import 'package:flutter/material.dart';

class AnxietyDiaryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Anxiety Diary'),
      ),
      body: Center(
        child: Text('Anxiety diary page.'),
      ),
    );
  }
}